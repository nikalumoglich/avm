install ncurses-devel on fedora
install install mysql-devel on fedora

sudo apt install libtinfo-dev on debian based

export DB_HOST=127.0.0.1
export DB_USER=haskelluser
export DB_PASSWORD=haskellpassword
export DB_NAME=avm
export JWT_SECRET=jwt_secret
export AVM_PORT=3000
export SESSION_TIME=900
export AVM_VERSION=1.0.2
export AWS_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
export AWS_DEFAULT_REGION=sa-east-1
export AWS_S3_BUCKET=tiozao-avm